{
  "name": "gestao-nexus",
  "version": "0.1.0",
  "description": "Ferramenta de gestão e transparência para os sócios da Nexus",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "postinstall": "echo 'install raiz pronto'",
    "start": "npm run migrate --prefix backend && npm run seed --prefix backend && npm run start --prefix backend",
    "migrate": "npm run migrate --prefix backend",
    "seed": "npm run seed --prefix backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

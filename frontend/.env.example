# Em dev, o Vite já faz proxy de /api para o backend (localhost:3001),
# então você não precisa preencher nada aqui.
#
# Se quiser apontar o frontend para um backend diferente (ex: um staging),
# defina VITE_API_URL. Deixe vazio para usar o proxy do Vite.
VITE_API_URL=
